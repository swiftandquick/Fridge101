package edu.wit.mobileapp.fridge101;

import android.support.v4.app.Fragment;
import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

// Keeping this page, not sure if it's needed.  

public class Tab3ShoppingList_Freezer extends Fragment {

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
        View rootView = inflater.inflate(R.layout.tab3shoppinglist_freezer, container, false);
        return rootView;
    }
}